{
  "name": "@jacekpietal/dependency-injection",
  "version": "1.4.2",
  "author": "Tom Guillermin <tom@tomsdev.com>",
  "description": "Dependency Injection using eye-candy TypeScript decorators.",
  "main": "build/index.js",
  "typings": "build/index.d.ts",
  "dependencies": {
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.12.12",
    "chai": "^5.1.1",
    "mocha": "^10.4.0",
    "prettier": "^3.2.5",
    "sinon": "^18.0.0",
    "typescript": "^5.4.5"
  },
  "scripts": {
    "prebuild": "rm -rf build",
    "build": "tsc --build tsconfig.json",
    "postbuild": "find build -name '*.map' -delete",
    "test": "tsc --build tsconfig.test.json && mocha build/test",
    "format": "yarn prettier `find ./lib -name '*.ts'` --write",
    "format-test": "yarn prettier `find ./test -name '*.ts'` --write"
  },
  "engines": {
    "node": ">= 0.10.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Prozi/dependency-injection.git"
  },
  "keywords": [
    "dependency",
    "injection",
    "typescript",
    "annotation",
    "decorator",
    "framework"
  ],
  "files": [
    "README.md",
    "build/"
  ]
}
